# Render Environment Variables Template
# Copy these to your Render service environment variables

# === REQUIRED VARIABLES ===
# Google Gemini API Key (Get from: https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=your_gemini_api_key_here

# Database URL (Auto-generated by Render PostgreSQL service)
DATABASE_URL=postgresql://supervity_bank_cc_user:password@hostname:port/supervity_bank_cc

# === BACKEND CONFIGURATION ===
# CORS Origins (Update with your actual frontend URL)
CORS_ORIGINS=https://your-frontend-app.onrender.com

# Application Environment
APP_ENV=production

# Server Port (Render will set this automatically)
PORT=8000

# File Storage Paths
PDF_STORAGE_PATH=./sample_data
GENERATED_PDF_STORAGE_PATH=./generated_documents

# === FRONTEND CONFIGURATION ===
# API Base URL (Update with your actual backend URL)
NEXT_PUBLIC_API_BASE_URL=https://your-backend-app.onrender.com/api

# Node Environment
NODE_ENV=production

# API Timeout (optional)
NEXT_PUBLIC_API_TIMEOUT=30000

# Debug Mode (optional)
NEXT_PUBLIC_ENABLE_DEBUG=false

# === OPTIONAL BACKEND VARIABLES ===
# Gunicorn Configuration
GUNICORN_LOG_LEVEL=info
GUNICORN_TIMEOUT=120
WEB_CONCURRENCY=1
WORKER_CONNECTIONS=1000

# Logging
LOG_LEVEL=INFO
LOG_FILE=

# Email Configuration (if using email features)
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_FROM=
# MAIL_PORT=587
# MAIL_SERVER=
# MAIL_FROM_NAME=
